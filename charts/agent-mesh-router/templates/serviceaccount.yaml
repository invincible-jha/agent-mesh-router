{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-mesh-router.serviceAccountName" . }}
  labels:
    {{- include "agent-mesh-router.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
