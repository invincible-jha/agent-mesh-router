apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-mesh-router.fullname" . }}
  labels:
    {{- include "agent-mesh-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-mesh-router.selectorLabels" . | nindent 4 }}
