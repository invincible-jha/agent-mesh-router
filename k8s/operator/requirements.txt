# AumOS Kubernetes Operator — Python dependencies
# Tested with Python 3.12

# Kubernetes operator framework — declarative handlers for CR lifecycle events
kopf==1.37.2

# Official Kubernetes Python client
kubernetes==29.0.0

# Data validation for CR spec/status objects
pydantic>=2.0,<3.0

# Structured logging (JSON output for log aggregation)
structlog==24.1.0

# Async HTTP client used by health-check probes
httpx>=0.27,<1.0

# OpenTelemetry — traces emitted for each reconciliation loop
opentelemetry-sdk>=1.25,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.25,<2.0
opentelemetry-instrumentation-httpx>=0.46b0
